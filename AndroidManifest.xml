<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ac.bbt.sp2014f_groupd"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 第2画面（目標入力画面） -->

        <activity
            android:name=".SecondlayoutActivity"
            android:label="@string/title_activity_second" >
        </activity>

        <!-- 第3画面（実績入力画面） -->
        <activity
            android:name=".ThirdlayoutActivity"
            android:label="@string/title_activity_third" >
        </activity>

        <!-- 第4画面（統計表示画面） -->
        <activity
            android:name=".FourthlayoutActivity"
            android:label="@string/title_activity_fourth" >
        </activity>
        <activity
            android:name=".CreateDailymemoryHelper"
            android:label="@string/title_activity_create_dailymemory_helper" >
        </activity>
    </application>

</manifest>
